export function getMediaUpdateRequest(mediaId, update) {
  return { url: `/api/media/${encodeURIComponent(mediaId)}`, method: "PATCH", payload: update };
}

export function buildMediaPageClientScript() {
  return `
    const getMediaUpdateRequest = ${getMediaUpdateRequest.toString()};
    (() => {
      const labels = {
        upload: "\\uC5C5\\uB85C\\uB4DC", uploading: "\\uC5C5\\uB85C\\uB4DC \uC911...", requestFailed: "\\uC694\\uCCAD\\uC744 \uCC98\\uB9AC\\uD558\\uC9C0 \uBABB\\uD588\\uC2B5\\uB2C8\\uB2E4.",
        photo: "\\uC0AC\\uC9C4", video: "\\uB3D9\\uC601\\uC0C1", complete: "\\uC644\\uB8CC", failed: "\\uCC98\\uB9AC\\uD558\\uC9C0 \uBABB\\uD588\\uC2B5\\uB2C8\\uB2E4.", description: "\\uC124\\uBA85", tags: "\\uD0DC\\uADF8", save: "\\uC800\\uC7A5", cancel: "\\uCDE8\\uC18C", preview: "\\uBBF8\\uB9AC\\uBCF4\\uAE30", edit: "\\uC218\\uC815",
        deactivate: "\\uBE44\\uD65C\\uC131\\uD654", activate: "\\uD65C\\uC131\\uD654", active: "\\uD65C\\uC131", inactive: "\\uBE44\\uD65C\\uC131", noDescription: "\\uC124\\uBA85\\uC774 \uC5C6\\uC2B5\\uB2C8\\uB2E4.", noTags: "\\uD0DC\\uADF8 \uC5C6\\uC74C", noMedia: "\\uB4F1\\uB85D\\uB41C \uBBF8\\uB514\\uC5B4\\uAC00 \uC5C6\\uC2B5\\uB2C8\\uB2E4.", loading: "\\uBBF8\\uB514\\uC5B4\\uB97C \uBD88\\uB7EC\\uC624\\uB294 \uC911...",
        imageUploading: "\\uC0AC\\uC9C4\\uC744 \uCC98\\uB9AC\\uD558\\uACE0 \uC788\\uC2B5\\uB2C8\\uB2E4.", videoUploading: "\\uB3D9\\uC601\\uC0C1\\uC744 \uCC98\\uB9AC\\uD558\\uACE0 \uC788\\uC2B5\\uB2C8\\uB2E4.", saved: "\\uBBF8\\uB514\\uC5B4 \uC815\\uBCF4\\uB97C \uC800\\uC7A5\\uD588\\uC2B5\\uB2C8\\uB2E4.", deactivated: "\\uBBF8\\uB514\\uC5B4\\uB97C \uBE44\\uD65C\\uC131\\uD654\\uD588\\uC2B5\\uB2C8\\uB2E4.", activated: "\\uBBF8\\uB514\\uC5B4\\uB97C \uD65C\\uC131\\uD654\\uD588\\uC2B5\\uB2C8\\uB2E4.", chooseFiles: "\\uC5C5\\uB85C\\uB4DC\\uD560 \uD30C\\uC77C\\uC744 \uC120\\uD0DD\\uD558\\uC138\\uC694.", finished: "\\uC5C5\\uB85C\\uB4DC \uCC98\\uB9AC\\uAC00 \uC644\\uB8CC\\uB418\\uC5C8\\uC2B5\\uB2C8\\uB2E4.", confirmDeactivate: "\\uC774 \uBBF8\\uB514\\uC5B4\\uB97C \uBE44\\uD65C\\uC131\\uD654\\uD560\\uAE4C\\uC694?"
      };
      Object.assign(labels, { experienceTags: "체험 태그", experienceNote: "체험 메모", noExperienceTags: "체험 태그 없음", noExperienceNote: "체험 메모 없음", dailyAsset: "Daily" });
      const uploadForm = document.querySelector("#media-upload-form"); const fileInput = document.querySelector("#media-files"); const uploadButton = document.querySelector("#media-upload-button"); const uploadFeedback = document.querySelector("#media-upload-feedback"); const uploadResults = document.querySelector("#media-upload-results"); const list = document.querySelector("#operator-media-list"); const listFeedback = document.querySelector("#operator-media-feedback"); let uploading = false;
      function setUploadFeedback(message, tone = "") { uploadFeedback.textContent = message || ""; uploadFeedback.className = "app-media-feedback " + tone; }
      function setUploadBusy(value) { uploading = value; uploadButton.disabled = value; uploadButton.textContent = value ? labels.uploading : labels.upload; }
      async function requestApi(url, options = {}) { let response; try { response = await fetch(url, options); } catch { throw new Error(labels.requestFailed); } let data; try { data = await response.json(); } catch { throw new Error(labels.requestFailed); } if (!response.ok || data.ok === false) throw new Error(data.error || labels.requestFailed); return data; }
      function requestOptions(descriptor) { return { method: descriptor.method, headers: { "content-type": "application/json" }, body: JSON.stringify(descriptor.payload) }; }
      function isSafePreviewUrl(value) { return typeof value === "string" && /^\\/media\\/[A-Za-z0-9_-]+$/u.test(value); }
      function formatDate(value) { const date = new Date(value); return Number.isNaN(date.getTime()) ? "" : date.toLocaleString("ko-KR"); }
      function createButton(label, className, handler) { const button = document.createElement("button"); button.type = "button"; button.className = className; button.textContent = label; button.addEventListener("click", handler); return button; }
      function renderUploadResults(results) { uploadResults.replaceChildren(); for (const result of results) { const item = document.createElement("li"); item.textContent = String(result.fileName || "") + " — " + (result.status === "success" ? labels.complete : (result.error || labels.failed)); uploadResults.append(item); } }
      function addPreview(card, media) { if (!isSafePreviewUrl(media.previewUrl)) return; if (media.kind === "video") { const video = document.createElement("video"); video.className = "app-media-preview"; video.controls = true; video.preload = "metadata"; video.src = media.previewUrl; card.append(video); return; } const image = document.createElement("img"); image.className = "app-media-preview"; image.src = media.previewUrl; image.alt = media.description || labels.photo; card.append(image); }
      function addEditForm(card, media, onSaved) { const form = document.createElement("form"); form.className = "app-media-edit-form"; const descriptionLabel = document.createElement("label"); descriptionLabel.textContent = labels.description; const description = document.createElement("textarea"); description.value = media.description || ""; descriptionLabel.append(description); const tagsLabel = document.createElement("label"); tagsLabel.textContent = labels.tags; const tags = document.createElement("input"); tags.type = "text"; tags.value = Array.isArray(media.tags) ? media.tags.join(", ") : ""; tagsLabel.append(tags); const experienceTagsLabel = document.createElement("label"); experienceTagsLabel.textContent = labels.experienceTags; const experienceTags = document.createElement("input"); experienceTags.type = "text"; experienceTags.value = Array.isArray(media.experienceTags) ? media.experienceTags.join(", ") : ""; experienceTagsLabel.append(experienceTags); const experienceNoteLabel = document.createElement("label"); experienceNoteLabel.textContent = labels.experienceNote; const experienceNote = document.createElement("textarea"); experienceNote.value = media.experienceNote || ""; experienceNoteLabel.append(experienceNote); const actions = document.createElement("div"); actions.className = "app-media-actions"; const save = createButton(labels.save, "app-media-button primary", async () => { save.disabled = true; try { const descriptor = getMediaUpdateRequest(media.id, { description: description.value, tags: tags.value.split(",").map((tag) => tag.trim()).filter(Boolean), experienceTags: experienceTags.value.split(",").map((tag) => tag.trim()).filter(Boolean), experienceNote: experienceNote.value }); await requestApi(descriptor.url, requestOptions(descriptor)); await onSaved(labels.saved); } catch (error) { save.disabled = false; listFeedback.textContent = error.message; } }); actions.append(save, createButton(labels.cancel, "app-media-button", () => form.remove())); form.append(descriptionLabel, tagsLabel, experienceTagsLabel, experienceNoteLabel, actions); card.append(form); }
      function renderMedia(mediaItems) { list.replaceChildren(); if (!mediaItems.length) { listFeedback.textContent = labels.noMedia; return; } listFeedback.textContent = ""; for (const media of mediaItems) { const card = document.createElement("article"); card.className = "app-media-card" + (media.active ? "" : " is-inactive"); addPreview(card, media); const content = document.createElement("div"); content.className = "app-media-content"; const heading = document.createElement("h3"); heading.textContent = labels.dailyAsset + " · " + (media.kind === "video" ? labels.video : labels.photo); const state = document.createElement("p"); state.className = "app-media-meta"; state.textContent = (media.active ? labels.active : labels.inactive) + (formatDate(media.updatedAt) ? " · " + formatDate(media.updatedAt) : ""); const description = document.createElement("p"); description.className = "app-media-description"; description.textContent = media.description || labels.noDescription; const tags = document.createElement("p"); tags.className = "app-media-tags"; tags.textContent = Array.isArray(media.tags) && media.tags.length ? media.tags.join(" · ") : labels.noTags; const experience = document.createElement("div"); experience.className = "app-media-experience"; const experienceTags = document.createElement("span"); experienceTags.textContent = labels.experienceTags + ": " + (Array.isArray(media.experienceTags) && media.experienceTags.length ? media.experienceTags.join(" · ") : labels.noExperienceTags); const experienceNote = document.createElement("p"); experienceNote.className = "app-media-experience-note"; experienceNote.textContent = labels.experienceNote + ": " + (media.experienceNote || labels.noExperienceNote); experience.append(experienceTags, experienceNote); const actions = document.createElement("div"); actions.className = "app-media-actions"; actions.append(createButton(labels.preview, "app-media-button", () => { const preview = card.querySelector("img, video"); if (preview) preview.scrollIntoView({ behavior: "smooth", block: "center" }); }), createButton(labels.edit, "app-media-button", () => addEditForm(card, media, async (message) => { await loadMedia(); listFeedback.textContent = message; })), createButton(media.active ? labels.deactivate : labels.activate, "app-media-button", async () => { if (media.active && !window.confirm(labels.confirmDeactivate)) return; try { const descriptor = getMediaUpdateRequest(media.id, { active: !media.active }); await requestApi(descriptor.url, requestOptions(descriptor)); await loadMedia(); listFeedback.textContent = media.active ? labels.deactivated : labels.activated; } catch (error) { listFeedback.textContent = error.message; } })); content.append(heading, state, description, tags, experience, actions); card.append(content); list.append(card); } }
      async function loadMedia() { list.replaceChildren(); listFeedback.textContent = labels.loading; try { const data = await requestApi("/api/media"); renderMedia(Array.isArray(data.media) ? data.media : []); } catch (error) { listFeedback.textContent = error.message; } }
      uploadForm.addEventListener("submit", async (event) => { event.preventDefault(); if (uploading) return; if (!fileInput.files || !fileInput.files.length) { setUploadFeedback(labels.chooseFiles, "error"); return; } const hasVideo = Array.from(fileInput.files).some((file) => file.type === "video/mp4"); setUploadBusy(true); uploadResults.replaceChildren(); setUploadFeedback(hasVideo ? labels.videoUploading : labels.imageUploading); try { const data = await requestApi("/api/media/upload", { method: "POST", body: new FormData(uploadForm) }); renderUploadResults(Array.isArray(data.results) ? data.results : []); setUploadFeedback(labels.finished, "success"); uploadForm.reset(); await loadMedia(); } catch (error) { setUploadFeedback(error.message, "error"); } finally { setUploadBusy(false); } });
      loadMedia();
    })();
  `;
}
